import DashboardSkeleton from "../../ui/skeletons"

// The (Overview) folder is to make set the pages where the skeleton will be shown

export default function Loading() {
    return <DashboardSkeleton />
}